# Railway ti fornisce DATABASE_URL
DATABASE_URL=postgresql://user:pass@host:port/db

# Obbligatorio per JWT
JWT_SECRET=change-me

# In Railway spesso serve SSL; lascia default (rejectUnauthorized: false)
# Per disabilitare ssl (solo locale):
# PGSSLMODE=disable

PORT=3000
